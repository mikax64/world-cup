import { SortRankingPipe } from './sort-ranking.pipe';

describe('SortRankingPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRankingPipe();
    expect(pipe).toBeTruthy();
  });
});
